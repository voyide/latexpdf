name: pronotes_latex
description: Brutalist Hybrid LaTeX and Visual PDF Editor.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  flutter_markdown: ^0.6.18+3
  flutter_math_fork: ^0.7.1
  pdf: ^3.10.7
  printing: ^5.11.1
  markdown: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
